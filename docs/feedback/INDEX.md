# Feedback index

2 report(s) — generated by `build_feedback_index.py <dir>`; do not hand-edit. Each entry is a report stem with its numbered proposals and its §Misses/§Friction bullet stubs; grep here for an `extends` target before restating a finding.

## 2026-07-04-plugin-serving-blind-probe
- `2026-07-04-plugin-serving-blind-probe#1` — Stop MCP-served subprocesses from hanging the client (miss above): every subprocess convoy spawns while serving over stdio — `interface/scaf
- `2026-07-04-plugin-serving-blind-probe#2` — Resolve the `[review].blocking` inert-field footgun (miss above): make it **optional** (`default false`) in `core/spec.py` and mark it "rese
- `2026-07-04-plugin-serving-blind-probe#3` — Add a re-run affordance (miss above): a `convoy run --fresh` / `convoy_run(reset=true)` that resets the workspace to `base` and deletes the 
- `2026-07-04-plugin-serving-blind-probe#4` — Give runtime failures a structured shape: a could-not-start real run returns `error` as an opaque string, unlike the located `problems` list
- `2026-07-04-plugin-serving-blind-probe#5` — Guard concurrent runs on one workspace: currently unsupported and unguarded (two runs would corrupt the tree silently). A workspace lock tha
- `2026-07-04-plugin-serving-blind-probe#6` — Consider a launch-and-poll mode for `convoy_run`: a real run blocks the MCP call for minutes-to-hours (it spawns nested agents). A backgroun
- `2026-07-04-plugin-serving-blind-probe §Friction` — `[[checks]]` is a required section with a ≥1-entry rule, but that requirement is only discoverable by tripping it (`missing required section
- `2026-07-04-plugin-serving-blind-probe §Friction` — The CLI tests monkeypatch `cli.run_series` at its import site, so extracting the shared service required moving 8 patch targets to `run_serv
- `2026-07-04-plugin-serving-blind-probe §Friction` — After `uv sync` pulled the new `mcp` dependency, the freshly-written `pytest.exe`/`ty.exe` console-script shims were blocked by a Windows ap
- `2026-07-04-plugin-serving-blind-probe §Misses` — **[BLOCKER] `convoy_init` hangs the MCP client on Windows — and by strong inference so does a real `convoy_run`.** Driving the *installed* p
- `2026-07-04-plugin-serving-blind-probe §Misses` — **[MED] `[review].blocking` is a required field that is inert in v1** — the driver never reads it (only `dump_series` does); the merge-block
- `2026-07-04-plugin-serving-blind-probe §Misses` — **[LOW] Re-run leaves the workspace un-resettable.** A completed or halted `convoy_run` leaves the `integration` and PR branches behind; a r

## 2026-07-05-polish-convoy-engine
- `2026-07-05-polish-convoy-engine#1` — Launch-and-poll mode for `convoy_run` — extends
- `2026-07-05-polish-convoy-engine#2` — re-run reset (#3) and workspace lock (#5) — extends
- `2026-07-05-polish-convoy-engine#3` — Add a `--workspace` option to the CLI `run` so it does not force `cwd == workspace`
- `2026-07-05-polish-convoy-engine#4` — Close the spawn-parse test-design gap the two PR #4 bugs exposed: give the stub-based
- `2026-07-05-polish-convoy-engine#5` — Note the `input_tokens` cache-fold magnitude prominently in `SKILL.md` / `02-formats.md`
- `2026-07-05-polish-convoy-engine#6` — Design docs that mirror code (`# src/...` blocks in `01-gate.md`) drift silently — the
- `2026-07-05-polish-convoy-engine §Friction` — **[MED] A real run blocks the caller for its full duration with no progress signal.** The run took
- `2026-07-05-polish-convoy-engine §Friction` — **[LOW] The CLI `run` forces `cwd == workspace`** (no `--workspace` flag), while the MCP tool
- `2026-07-05-polish-convoy-engine §Friction` — **[LOW] `spawn_complete.input_tokens` is dominated by folded-in cache reads** (3.0M / 2.5M on the
- `2026-07-05-polish-convoy-engine §Misses` — **[MED] Two real spawn-parse bugs the unit tests never exercised.** `num_turns` silently reported
- `2026-07-05-polish-convoy-engine §Misses` — **[MED] Doc-vs-code drift the docs' own review never caught.** `telemetry.py` said "three v1
- `2026-07-05-polish-convoy-engine §Misses` — **[MED] The `review` governance role is required-but-inert in v1** — `[governance.budgets].review`
